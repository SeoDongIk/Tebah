package com.example.data.source.preference

// 유저 로그인 상태, 기본 설정 저장하기.

interface LocalUserPreferenceDataSource {
//    suspend fun saveUserProfile(user: User)
//    suspend fun getUserProfile(): User?
}